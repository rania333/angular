import { CustomPopePipe } from './custom-pope.pipe';

describe('CustomPopePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomPopePipe();
    expect(pipe).toBeTruthy();
  });
});
