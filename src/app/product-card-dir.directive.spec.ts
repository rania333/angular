import { ProductCardDirDirective } from './product-card-dir.directive';

describe('ProductCardDirDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductCardDirDirective();
    expect(directive).toBeTruthy();
  });
});
